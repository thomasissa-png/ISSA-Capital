# Lessons Learned — ISSA Capital

> Format v2 (11 colonnes). P0/P1 propagés avant clôture. Entrées terminées archivées dans docs/lessons-learned-archive.md.
> Sessions Cadrage, 4-11 : toutes les entrées sont terminées et archivées (audit TTL session 14).

## Session 12 — 2026-05-12 — Propagation évaluée S15

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 12 | 2026-05-12 | problème | P2 | **Drift reporting @qa : tests ajoutés mais reportés "déjà committés"** — @qa appelé pour ajouter 3 tests a réellement modifié les fichiers mais son rapport affirmait "déjà en place". L'orchestrateur a détecté le mismatch via `git status` et committé manuellement. | Commit manuel de l'orchestrateur après vérif `git status`. | Tout agent producteur DOIT terminer son rapport handoff en exécutant `git log -1 --stat` et en collant le sha + diff stats. Si le rapport dit "fait" mais qu'aucun commit n'est traçable → orchestrateur force le commit. | agent (@qa, @fullstack, agent-factory) | qa.md, fullstack.md, _base-agent-protocol.md | fait | n/a (fichiers Gradient framework-managed, écrasés aux updates — learning retenu en mémoire organisationnelle) |
| Session 12 | 2026-05-12 | problème | P2 | **Audit @legal localisation imprécise — fix pointe en aval au lieu de l'amont** — Audit P0 #1 pointait `pdf-quittance.ts:164` alors que le builder amont peuplait DÉJÀ la variable avec la civilité. Faux positif partiel. | Test de non-régression ajouté. | Tout audit qui pointe une ligne de code DOIT tracer la chaîne de données amont avant de prononcer "FAIL". Ajouter étape "Grep amont" obligatoire. | agent (@legal, @reviewer) | legal.md, reviewer.md | fait | n/a (fichiers Gradient framework-managed) |
| Session 12 | 2026-05-12 | pattern | P2 | **Helper `extractPdfText` réutilisable** — zlib natif Node, décompression FlateDecode + parse opérateurs TJ. Actuellement dupliqué dans 2 fichiers de tests. | Helper créé inline dans 2 fichiers de tests. | Extraire dans `src/lib/test-utils/pdf-text.ts` partagé. | documentation + pattern | src/lib/test-utils/pdf-text.ts (à créer) | fait | n/a (TODO code @fullstack, pas une propagation de règle — tracé dans memo reprise) |
| Session 12 | 2026-05-12 | pref fondateur | P2 | **Thomas préfère livrer en vagues** — Sur 4 P0 audit légal, 2 actionnables sans Thomas, 2 bloqués par décisions Thomas. Thomas a scope-limité : "Appliquons uniquement p0 #1 et #4". | Mission @fullstack scope-restreinte à P0 #1 et #4. | Regrouper recommandations en 2 colonnes : "actionnable sans décision Thomas" vs "nécessite arbitrage Thomas". | agent (@legal, @reviewer, orchestrator) + founder-prefs | legal.md, reviewer.md, docs/founder-preferences.md, orchestrator.md | fait | propag (founder-preferences.md vérifié S15 — fichiers Gradient = framework-managed) |

## Session 13 — 2026-05-12 — Propagation évaluée S15

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 13 | 2026-05-12 | problème | P1 | **#91 API Anthropic publique ne supporte pas `input_audio`** — Presume que Sonnet/Haiku transcrit l'audio nativement. C'est faux : seul Claude Code (CLI) le fait. L'API renvoie `400 invalid_request_error: unknown content block type input_audio`. | Switch vers OpenAI Whisper API. Pipeline : Whisper (audio → texte FR) puis Haiku 4.5 (texte → JSON). | **VÉRIFIER les capacités exactes de l'API AVANT de promettre une fonctionnalité.** L'audio input n'est PAS supporté par l'API Anthropic publique. Pour transcription : Whisper OpenAI (~$0.006/min) ou STT externe. | règle-globale + agents | CLAUDE.md, ia.md, fullstack.md | fait | propag |
| Session 13 | 2026-05-12 | problème | P1 | **#92 Telegram iOS strip systématiquement les EXIF des photos HEIC** — En mode "Send as file" depuis Photos iOS, Telegram passe par PHPhotoLibrary qui convertit HEIC→JPEG côté client ET strip les métadonnées AVANT envoi. **5h de tâtonnement** sur 8 commits. | Abandon extraction EXIF. Workflow `inbox-photo-batch` livre : photos bufférisées 5s → Anya demande la date à Thomas via boutons. | **Ne jamais sur-engineering pour récupérer une donnée perdue à la source.** Pattern "Anya demande l'info manquante" plus fiable que tâtonnement extraction. | règle-globale + pattern | CLAUDE.md (règle 25), fullstack.md, ia.md | fait | propag |
| Session 13 | 2026-05-12 | insistance | P1 | **#93 8 commits photos en mode "essai-erreur" sans test local** — Thomas (verbatim) : "jen ai marre de tâtonner, sois sur" puis "12e commit sur ce sujet". 8 commits successifs sans tester localement. | Test local sur vrai fichier AVANT push valide. | **Avant tout push d'un fix sur un bug reproductible, TESTER en local.** Si 2 iterations sans succès → STOP push, demander un sample, écrire un test, valider, PUIS pusher. Règle : "0 commit hypothétique sur un bug récurrent." | règle-globale + agents | CLAUDE.md (règle 26), fullstack.md, qa.md, _base-agent-protocol.md | fait | propag |
| Session 13 | 2026-05-12 | pref fondateur | P1 | **#94 Thomas refuse les services gratuits qui nécessitent un billing account** — Google STT gratuit ≤60 min/mois MAIS exige activation billing GCP. Thomas a refusé. Préférence : services standalone (clé API simple). | Switch Google STT → OpenAI Whisper ($0.006/min, ~0,03 EUR/mois). | **Pour tout choix de service externe gratuit/cloud, vérifier d'abord s'il exige un billing account.** Si oui, proposer alternative standalone. | founder-prefs + agents | docs/founder-preferences.md, fullstack.md, infrastructure.md, ia.md | fait | propag |
| Session 13 | 2026-05-12 | pref fondateur | P2 | **Thomas préfère contrôle explicite par boutons > décision auto de l'IA** — Pour inbox-message-router, Thomas a choisi A (boutons à chaque fois). Pattern : pour décisions ou risque d'erreur non-nul, Thomas préfère 1 clic supplémentaire a un faux positif silencieux. | Workflow livre avec 3 boutons Calendar/Tâches/Annuler à chaque texte court. | **Quand on propose "Anya decide auto vs Thomas valide", proposer A (Thomas valide) en option par défaut.** | founder-prefs | docs/founder-preferences.md | fait | propag (vérifié S15 — déjà dans founder-preferences.md) |
| Session 13 | 2026-05-12 | pref fondateur | P2 | **Modèle LLM adapté à la tâche** — Haiku 4.5 pour extraction JSON simple (~80% économie + ~2x plus rapide). Nuance "Sonnet par défaut" : modèle adapté à la tâche. | Switch Sonnet → Haiku 4.5 pour inbox-message-router. CR reste Sonnet 4.6. | **Pour chaque nouvel appel LLM, justifier le choix de modèle par la complexité de la tâche.** Haiku OK pour extraction JSON, Sonnet pour génération texte structuré. | founder-prefs + agents | docs/founder-preferences.md, ia.md, fullstack.md | fait | propag (vérifié S15 — déjà dans founder-preferences.md ; ia.md/fullstack.md = Gradient framework-managed) |
| Session 13 | 2026-05-12 | pattern | P2 | **Détection format par signature bytes > MIME** — Webhook Telegram envoie parfois des MIME mensongers. Solution : détecter le format par les premiers bytes du buffer. | Fonction `isHeicBuffer(buf)` vérifié bytes 0-1, 4-7, 8-24. Ignore le MIME. | **Pour tout dispatch par type de media dans un webhook, détecter le format par signature bytes plutôt que MIME.** | agents | fullstack.md, ia.md | fait | n/a (fichiers Gradient framework-managed — pattern documenté dans dev-decisions.md S13 section EXIF) |

## Session 14 — 2026-05-17 — Propagation complète S15

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 14 | 2026-05-17 | pattern | P0 | **#95 MCP Drive accessible : lire le vault avant de poser des questions** — Claude Code a accès au vault Drive Thomas en direct via MCP Drive (`mcp__00415231-e65d-436c-84ee-f10eaab8da71__*`). Game-changer architectural : Anya et tous les agents peuvent lire le vault en live au lieu de dépendre de caches statiques. Thomas a explicitement signalé l'anti-pattern : "tes questions m'ennuient car toutes les réponses sont dans le vault". Le vault Drive est la **source de vérité absolue**. | Découverte MCP Drive S14. Lecture confirmée sur Thomas Issa.md, Emmanuel Gomez.md, Léo Fanorenantsoa.md, Lucas Geoffroy.md, READMEs vault. | **Ne JAMAIS demander à Thomas une info qui se trouve dans une fiche, un README ou un index du vault.** Avant toute question, lire le vault via MCP Drive. Si l'info n'est PAS dans le vault → alors seulement demander. Le vault est la source de vérité unique pour les contacts, la structure, les fiches, les documents. Migration cache statique → lecture live planifiée en 5D. | règle-globale + agents | CLAUDE.md (nouvelle règle R1), orchestrator.md, fullstack.md, ia.md, _base-agent-protocol.md | fait | propag |
| Session 14 | 2026-05-17 | pref fondateur | P1 | **#96 TTL pendings interactifs : minimum 7 jours** — TTL initial de 24h pour les pendings Telegram (cartes de validation Thomas) était trop court. Thomas peut partir en week-end ou en vacances sans pouvoir valider. Découvert quand les cartes expiraient avant que Thomas ne les voie. | TTL pending-store modifié 24h→7j dans `pending-store.ts`. Commit `28a7e12` + `f315a59`. | **Tout état interactif que Thomas doit valider manuellement : TTL minimum 7 jours.** Pas de TTL < 72h sur un état que Thomas doit valider. Usage humain = week-ends, vacances, imprévus. Le coût d'un pending expiré (re-traitement) >> coût d'un pending qui traîne 7j. | code + documentation | docs/dev-decisions.md (entrée ajoutée S15), CLAUDE.md (R3), pending-store.ts | fait | propag |
| Session 14 | 2026-05-17 | problème | P1 | **#97 Dispatch webhook oublié pour nouveau callback Telegram** — Le Lot 2 (4D-2) a livré `handleNoMatchCallback` avec le préfixe `email_nomatch:` mais a oublié d'ajouter le dispatch correspondant dans `webhook/route.ts`. Résultat : les callbacks no-match tombaient en cascade dans le router CR S13, déclenchant le mauvais workflow. Bug détecté en revue prod. | Ajout dispatch `email_nomatch:` dans `webhook/route.ts`. Commit `e07eb0f`. | **Tout nouveau préfixe de callback Telegram DOIT avoir un dispatch dans `webhook/route.ts` testé end-to-end.** Checklist obligatoire : (1) handler créé, (2) dispatch ajouté dans webhook/route.ts, (3) test E2E callback→handler. Gate candidate G33 (voir évaluation promotions). | checklist intégration + gate candidate | CLAUDE.md (R4 checklist), _gates.md (G33 ajoutée), webhook/route.ts | fait | propag |
| Session 14 | 2026-05-17 | pattern | P0 | **#98 Connecteurs MCP natifs Claude AVANT setup technique custom** — Le MCP Drive était disponible toute la session S14, mais Claude ne l'a réalisé qu'après un prompt explicite de Thomas. Confirme la règle 19 CLAUDE.md (connectors natifs avant MCP custom) et l'étend : **vérifier les MCP disponibles en début de chaque session**. Deux récidives antérieures (Gmail/Calendar S9, Asana/Craft S10). | Lecture vault MCP Drive confirmée S14. | **En début de chaque session, lister les MCP disponibles et vérifier lesquels donnent accès à des données pertinentes pour le projet.** Ne pas attendre que Thomas signale un MCP — le scanner proactivement. Règle 19 CLAUDE.md déjà existante, à renforcer avec "scan proactif en début de session". | règle-globale + agents | CLAUDE.md (R2 scan MCP), orchestrator.md, agent-factory.md, _base-agent-protocol.md | fait | propag |

## Session 15 — 2026-05-18 — Propagation à évaluer S16

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 15 | 2026-05-18 | problème | P0 | **#99 Édition fichier Drive : PATCH in-place, JAMAIS create+delete** — Sur 6 fiches contacts vault à enrichir, choix initial create+delete (Zapier `newtxtfile` + `delete_file`). Résultat : changement de fileId pour les 6 fiches → wikilinks Obsidian cassés, suffixes `(1)` côté cache local, mimeType `text/plain` au lieu de `text/markdown`. Thomas (verbatim) : "je ne comprends pas pourquoi tu n'as pas just editer les fiches !!!!!!!!!". | Méthode PATCH validée : Zapier `_zap_raw_request PATCH` vers `/upload/drive/v3/files/{fileId}?uploadType=media`. Préserve fileId + corrige mimeType. 5 fiches re-PATCHées avec succès. Documenté dans `docs/drive-edit-strategy.md`. | **Pour toute modification d'un fichier Drive existant : PATCH in-place via `_zap_raw_request`.** Le pattern create+delete est INTERDIT — il casse les références par fileId (wikilinks Obsidian, liens directs Drive, partages). Tester la méthode PATCH avant tout fallback destructif. | règle-globale + agents | CLAUDE.md (règle 27 candidate), drive-edit-strategy.md, fullstack.md, ia.md | fait | à propager S16 |
| Session 15 | 2026-05-18 | problème | P1 | **#100 Test sur 1 fichier AVANT batch (vault)** — Test Carl OK (1 fiche) → enchaîné direct sur 5 fiches en parallèle sans vérifier l'impact wikilinks. Aurait dû : test Carl → vérification Thomas dans Obsidian côté lui → puis seulement les 5 autres. Lien direct avec leçon #93 (S13). | Réparation a posteriori : 5 PATCH pour fix mimeType + delete fantôme + delete Thomas Issa de Contacts/. | **Pour toute opération en batch sur le vault Drive (n>1) : tester 1 fichier ET attendre validation Thomas (visuelle dans Obsidian) avant de lancer le batch.** Ne pas conclure "ça marche" après 1 test technique réussi côté Drive. | règle-globale | CLAUDE.md (étendre règle 26), fullstack.md | fait | à propager S16 |
| Session 15 | 2026-05-18 | pattern | P1 | **#101 Suppression dette technique : retirer le hardcoded dès que la source live fonctionne** — `BASE_CONTACTS` hardcodé est resté en fallback après 5F par prudence excessive. Thomas a tranché : "ne gardons pas la base hardcodée". Une fois les 6 fiches vault validées, retrait sans regret (commit `1f1992b`). | Suppression de `BASE_CONTACTS` dans `src/lib/secretariat/contacts.ts`. Tests adaptés. | **Quand une source live (vault) remplace un hardcoded, retirer le hardcoded dans le MÊME jalon ou immédiatement après validation source live.** Pas de dette technique "au cas où". Le fallback de sécurité au runtime est `try/catch` sur la source live → tableau vide, pas une copie statique. | règle-globale | CLAUDE.md, fullstack.md | fait | à propager S16 |
| Session 15 | 2026-05-18 | problème | P2 | **#102 Vérité TickTick : pas de refresh_token + pas de webhook public** — Code S15 initial supposait l'OAuth standard (refresh_token retourné, webhooks supportés). Réalité : TickTick émet seulement un access_token longue durée (180j) et n'expose AUCUN webhook. 2 fix S15.2.1 + S15.2.2 nécessaires. | Switch vers `TICKTICK_ACCESS_TOKEN` direct (pas de refresh). Polling via GitHub Actions cron 15min (au lieu de webhook). | **Avant de coder une intégration OAuth/webhook, vérifier la doc API officielle : OAuth flow exact (refresh ou pas), endpoints webhooks réels.** Ne pas présumer "OAuth standard + webhook standard". | règle-globale | ia.md, fullstack.md | fait | à propager S16 |
