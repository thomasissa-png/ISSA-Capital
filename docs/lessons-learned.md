# Lessons Learned — ISSA Capital

> Format v2 (11 colonnes). P0/P1 propagés avant clôture. Entrées terminées archivées dans docs/lessons-learned-archive.md.
> Sessions Cadrage, 4-16 : terminées et archivées (audit TTL S14 + S23) — toutes propagées dans CLAUDE.md R1-R12 + agents. Détail dans git history + docs/lessons-learned-archive.md.

## Session 24 — 2026-05-28 — chantier nouveaux contacts (carte WhatsApp + reply + polish + Lier + audit reviewer)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 24 | 2026-05-28 | pattern | P0 | **#126 Lancer un reviewer agent à la fin d'un chantier ≥ 3 PRs** — j'ai livré 7 PRs (#53-#59) sans recul, Thomas a demandé une revue → reviewer trouve **9 bugs réels** (PR #60). Le risque silencieux d'empilage de features sans audit critique est concret. | PR #60 corrige les 9 bugs. CLAUDE.md R13 ajoutée. | **Tout chantier ≥ 3 PRs liées DOIT déclencher un reviewer agent critique AVANT clôture de session, focus bugs/edge cases. La conformité gate (tests + tsc + lint + build) n'est pas suffisante : elle ne capte ni les races ni les YAML edge cases ni les corruptions silencieuses.** | règle-globale | CLAUDE.md (R13) | fait | propagé |
| Session 24 | 2026-05-28 | problème | P0 | **#127 Race save → send → re-save sur les pendings Telegram** — l'ordre « save initial → send carte → re-save avec messageId » ouvre une fenêtre (~300 ms) où un reply ultra-rapide arrive avant la 2e save et tombe dans le pipeline normal → fausse tâche TickTick. | Ordre corrigé : **send d'abord → save UNE FOIS avec messageId**. Si send échoue, on ne save pas du tout (évite pollution TTL 7j). | **Tout pattern « save → I/O externe qui retourne un id → re-save avec id » est une race. Inverser : appel externe d'abord, save unique avec id. Si l'appel échoue, on ne sauve pas (sinon l'entrée est orpheline et inutilisable).** | documentation | docs/lessons-learned.md (#127) | fait | propagé |
| Session 24 | 2026-05-28 | problème | P0 | **#128 YAML flow-style silencieusement corrompu par un parser ligne-par-ligne bloc-style** — `addToFrontmatterList` insérait `  - new@x.fr` sous une ligne `alias_email: [a@b, c@d]` → YAML invalide, Obsidian Dataview throw. Bypass de l'idempotence. | Détection `afterColon.startsWith('[')` → refus d'édition (return content inchangé + warn). Tests dédiés. | **Tout helper qui patche du YAML doit détecter le flow-style (`[...]`/`{...}`) et REFUSER l'édition plutôt que corrompre. Préférer un fail-safe explicite à un YAML cassé silencieusement.** | documentation | docs/lessons-learned.md (#128) | fait | propagé |
| Session 24 | 2026-05-28 | problème | P1 | **#129 Polish LLM tronqué silencieusement → red line « préserver toutes les infos » violée** — MAX_TOKENS=512 trop bas sur Haiku, le polish s'arrêtait à `… il a une fille de 4 a` perdant le tél/email à la fin. Pas de détection. | Bump MAX_TOKENS=2048 + heuristique `looksTruncated` (fin sans ponctuation finale `.!?:")]'»` → fallback texte brut). Tests dédiés. | **Tout LLM avec MAX_TOKENS cap doit avoir une détection de troncature post-hoc + fallback. La red line « zéro invention/perte » ne peut pas reposer sur la confiance dans le cap.** | règle-globale + ia | ia.md, docs/lessons-learned.md (#129) | fait | non-propagé |
| Session 24 | 2026-05-28 | problème | P1 | **#130 Reply à carte Telegram expirée → tâche TickTick fantôme** — Thomas reply à une carte déjà cliquée/expirée, le texte tombait dans `parseAddTaskFromText` → todo aléatoire genre "oui c'est le maxime de chez versi". | Détection : `reply_to_message.text` contient « Contact inconnu » + aucun pending matché → ack « carte expirée, utilise `/enrichir` » + return. Guard transverse pour vocal/photo aussi. | **Tout handler de reply Telegram doit vérifier si le message d'origine ressemble à un pending connu (texte caractéristique) AVANT de tomber au pipeline générique. Sinon n'importe quel reply orphelin devient une todo fantôme.** | documentation | docs/lessons-learned.md (#130) | fait | non-propagé |
| Session 24 | 2026-05-28 | problème | P1 | **#131 Choix silencieux du premier match floue = mauvais lien irréversible** — `matchContacts(nameFrom)` peut renvoyer 2+ homonymes (« Maxime Lemoine » edhec vs autre), je prenais silencieusement `matches[0]` → risque réel de lier au mauvais Maxime. | `existingMatchHint` (singulier) → `existingMatchHints` (array, cap 3 boutons). Si >3 : warning textuel seul, pas de bouton. **Jamais de choix silencieux.** | **Recherche floue + écriture irréversible = TOUJOURS afficher TOUTES les options au user, jamais le 1er résultat silencieusement. Si la liste est trop longue, refuser le clic et exiger l'action manuelle.** | règle-globale + ux + fullstack | ux.md, fullstack.md, docs/lessons-learned.md (#131) | fait | non-propagé |
| Session 24 | 2026-05-28 | pref fondateur | P0 | **#132 Thomas préfère l'audit critique demandé explicitement aux livraisons en série sans retour** — « revoie tout ce que tu as fait et verifie ni bug ni trou dans la raquette :) » après 7 PRs. Indique qu'il attend des cycles audit/correction, pas une livraison linéaire. | Reviewer agent lancé → 9 bugs trouvés → PR #60 corrige. Pattern à institutionnaliser. | **Thomas valorise les cycles « livraison → audit critique → correction » autant que la livraison elle-même. Anticiper : pour tout chantier ≥ 3 PRs, glisser une étape audit reviewer DANS le scope.** | founder-prefs | docs/founder-preferences.md | fait | propagé |
| Session 24 | 2026-05-28 | pref fondateur | P1 | **#133 Thomas refuse les options A/B/C même sur une feature complexe, dès que l'intent est clair** — verbatim S24 multiple : « Go ! Pas de soucis pour le volume » + « Rajoute la possibilité d'ajouter un contact existant » + « Anya doit me proposer ». Cohérent avec R12 mais reconfirmé sur features non-triviales (5 priorités d'audit → 4 PRs sans demander confirmation à chaque). | Implémenté les 4 PR (#57-#60) en autonomie, batch. | **Confirmer l'application littérale de R12 : intent clair Thomas = action immédiate sans demander, signaler résultat. Même si la feature est complexe (architecture, 5 priorités à séquencer).** | founder-prefs | docs/founder-preferences.md | confirmé | non-propagé |
| Session 24 | 2026-05-28 | pref fondateur | P1 | **#134 Thomas verbalise les contraintes (« je ne veux pas que le texte soit inséré tel quel »), pas les solutions** — formulation « si jamais je ne veux pas que X » + « il faut que je puisse Y ». À moi de proposer le mécanisme (LLM polish, bouton de prévisualisation, etc.). | Polish LLM Haiku avec garde-fous + preview dans message de succès. | **Thomas formule des CONTRAINTES, pas des spécifications techniques. À l'agent de proposer 1 implémentation cohérente avec les contraintes ET les patterns existants — sans demander confirmation sur la solution (juste sur le scope).** | founder-prefs | docs/founder-preferences.md | confirmé | non-propagé |
| Session 24 | 2026-05-28 | pattern | P1 | **#135 Pattern UX « confirmation 2 étapes » sur action irréversible côté Drive** — bouton « Lier » écrit dans une fiche existante (irréversible). Confirmation 2 étapes (« Confirmer ? [Oui] [Annuler] ») avant écriture, avec restauration de la carte d'origine au cancel. | Implémenté pour bouton Lier email + WhatsApp. | **Toute action one-click qui écrit irréversiblement sur le vault Drive doit être en 2 étapes : 1er clic → carte de confirmation ; 2e clic → action. Annuler doit restaurer l'état d'origine sans perte.** | pattern + ux | ux.md, docs/lessons-learned.md (#135) | fait | non-propagé |
| Session 24 | 2026-05-28 | problème | P2 | **#136 Invalidate cache contacts après chaque écriture vault contact** — `getVaultContacts()` TTL 1h. Si Thomas vient de créer une fiche via le bouton de carte et qu'un autre mail arrive 10 min après, la détection homonyme peut louper (cache stale). | `invalidateContactCachesAfterWrite()` (invalide les 2 caches : email-ingest + vault-contacts) appelé après chaque création/lien réussi. | **Tout cache de lecture qui sert un flux d'écriture (TTL > 0) doit être invalidé synchroniquement après l'écriture. Sinon le « stale » contamine le run suivant.** | documentation + fullstack | fullstack.md (à enrichir), docs/lessons-learned.md (#136) | fait | non-propagé |

## Session 23 — 2026-05-27 — réparation prod Anya (Outlook/WhatsApp/TickTick/hot-context) + édition vault en place

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 23 | 2026-05-27 | problème | P0 | **#120 markProcessed manquant → emails « intéressants » re-traités en boucle (backlog Outlook jamais drainé)** — chemin normal (non-spam/non-système) de `processOneEmail` ne marquait JAMAIS l'email traité → re-listé + re-brouillonné chaque run, run saturé avant d'atteindre Outlook (25/7 jamais drainés, 0 brouillon). | `markProcessed` final + cap `MAX_EMAILS_PER_SOURCE_PER_RUN=8`. **Prouvé prod** (brouillon Outlook créé). | **Tout item consommé d'une file (email/message) DOIT être marqué traité dans le MÊME passage, sinon boucle + famine des sources suivantes. Borner le batch par run.** | documentation + fullstack | Workflow Email Ingest.md (vault, red line) + dev-decisions.md | fait | propagé (fiche vault + mémo) |
| Session 23 | 2026-05-27 | pref fondateur | P0 | **#122 Thomas veut l'autonomie, pas le nag** — verbatim « ça ne devrait pas dépendre de moi, on a tout ! ». Un garde-fou qui réclame une action manuelle récurrente (bumper une date) = mauvais design. | Garde-fou hot-context V0 (carte « bumper ») remplacé par **revue nocturne autonome 22h** (Sonnet écrit seul) + Telegram des changements. | **Quand Anya « a tout » pour faire le travail : le faire en AUTONOME + rapport a posteriori. Pas de tâche manuelle récurrente pour l'utilisateur. Validation seulement si risque/irréversible.** | founder-prefs | docs/founder-preferences.md | fait | propagé |
| Session 23 | 2026-05-27 | problème | P1 | **#121 Lecture SQLite WAL en `-readonly` échoue sans droit sur `-shm`** — « attempt to write a readonly database (8) » : un reader WAL doit écrire le `-shm`, or `thomas` n'a pas les droits sur `/root/.beeper` (setfacl non durable). | `snapshotDb` : copie `index.db`(+wal/shm) dans un tmp inscriptible, requête sur la copie. | **Lire une DB SQLite WAL sans droit d'écriture = snapshot vers tmp inscriptible avant requête. `-readonly` seul ne suffit pas en WAL.** | documentation | docs/dev-decisions.md | fait | propagé (mémo + fiche) |
| Session 23 | 2026-05-27 | problème | P1 | **#123 Exclusion par sous-chaîne trop large = filtre le business** — `BEEPER_EXCLUDE=sarani,ubi` filtrait quasi tous les chats actifs (chats d'affaires nommés « Sarani x UBI »). | Liste d'**inclusion prioritaire** `BEEPER_INCLUDE` qui prime sur l'exclusion. | **Toute exclusion par sous-chaîne de nom doit avoir une liste d'inclusion prioritaire (exceptions), sinon elle emporte des éléments légitimes. Mesurer le volume réellement filtré.** | documentation | docs/lessons-learned.md | fait | propagé |
| Session 23 | 2026-05-27 | problème | P1 | **#124 Édition vault : l'outil n8n « Update file » ne fait que RENOMMER** — testé, contenu inchangé ; il manquait un PATCH média. | Ajout 3 outils n8n (`Mettre_a_jour_contenu_fichier` PATCH `uploadType=media`, `Creer_fichier_vault`, `Lire_contenu_brut`). Quirk : erreur cosmétique « circular structure » même en succès → vérifier par relecture. | **Éditer le contenu d'un fichier Drive depuis un agent = PATCH `uploadType=media` (préserve fileId/wikilinks, R5). Vérifier EMPIRIQUEMENT qu'un outil « update » change le contenu (pas juste les métadonnées) avant de s'y fier.** | documentation + founder-prefs | docs/drive-edit-strategy.md, docs/founder-preferences.md | fait | propagé |
| Session 23 | 2026-05-27 | problème | P2 | **#125 Squash-merge : `git merge origin/main` réintroduit des fichiers supprimés** — le merge a re-ajouté des fichiers V0 que j'avais supprimés + des refs webhook. | Re-suppression `git rm -f` + re-strip refs après le merge, re-vérif par grep. | **Après `git merge origin/main` sur un repo en squash-merge : re-grep les suppressions/refs — le merge peut réintroduire des fichiers supprimés.** | documentation | docs/lessons-learned.md | fait | propagé (mémo) |

## Session 22 — 2026-05-25 — crons dépôt + hot-context + DeepSeek + MCP debug

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 22 | 2026-05-25 | problème | P1 | **#119 Connecteur MCP Cowork ≠ session Claude Code web** — serveur MCP debug VPS branché dans Cowork mais outils absents de ma session. Cause : (a) Cowork et Claude Code web = 2 surfaces ; (b) les MCP se chargent au DÉMARRAGE de session. | Diagnostic : ToolSearch ×3 (vide) + WebFetch domaine (404 = up) → « non enregistré côté env Claude Code, tester en session fraîche ». | **Un connecteur MCP doit être enregistré dans l'env Claude Code web (≠ Cowork) ET la session (re)démarrée. Avant de conclure « indisponible » : ToolSearch + WebFetch pour distinguer réseau vs non-enregistré.** | règle-globale + base-protocol | docs/lessons-learned.md (#119) | fait | n/a (mémoire orga) |

## Session 21 — 2026-05-22 — vault SOT + audit qualité skills + hotfix CR PDF

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 21 | 2026-05-22 | problème | P0 | **#113 Audit qualité critique non escaladé → bug prod** — trou #6 (JSON output non documenté SKILL.md vault) listé en rapport, non escaladé → Anya CR markdown, pas de PDF. | Suffixe runtime FORMAT JSON dans `loadCrSystemPrompt()`. | **Tout verdict audit critique/bloquant/NO-GO/régression = ESCALADE bloqueur pré-prod, JAMAIS listing rapport.** | règle-globale + orchestrator | CLAUDE.md (R10), orchestrator.md, reviewer.md | fait | propag (CLAUDE.md R10) |
| Session 21 | 2026-05-22 | problème | P0 | **#114 Sous-agents n'héritent pas des tools MCP** — `@fullstack` lancé pour resync Drive MCP n'a pas `mcp__*`. | Resync côté orchestrator. | **Mission Drive/Gmail/etc. = orchestrator-only OU pré-extraire le contenu MCP et l'inliner dans le prompt sous-agent.** | règle-globale + orchestrator | CLAUDE.md (R11), orchestrator.md, _base-agent-protocol.md | fait | propag (CLAUDE.md R11) |
| Session 21 | 2026-05-22 | insistance | P0 | **#115 Options A/B/C post-audit alors que brief limpide** — verbatim « Mon brief est clair, pourquoi tu m'interromps ? ». | Décision défaut « vault SOT » appliquée. | **Brief Thomas clair = décision immédiate par lecture littérale du verbatim. JAMAIS d'options A/B/C.** | règle-globale + orchestrator | CLAUDE.md (R12a), orchestrator.md | fait | propag (CLAUDE.md R12) |
| Session 21 | 2026-05-22 | problème | P1 | **#116 Diagnostic prod en aveugle au lieu de relire ses rapports** — verbatim « j'ai honte de tes questions ». | Hotfix trouvé en relisant le rapport @ia. | **Bug prod = scanner ses propres audits/rapports AVANT de questionner Thomas.** | règle-globale + orchestrator | CLAUDE.md (R12b), orchestrator.md | fait | propag (CLAUDE.md R12) |
| Session 21 | 2026-05-22 | problème | P1 | **#117 Audit « modifié date X = OK » au lieu d'audit ligne par ligne** — verbatim « Arrête de me faire répéter ». | Audit ligne par ligne refait (6 skills), 1 trou juridique `baux` trouvé. | **Audit qualité = lecture contenu + comparaison red lines + verdict GO/GAP/NO-GO. Un timestamp n'est pas un audit.** | règle-globale + reviewer | reviewer.md, qa.md, _base-agent-protocol.md | fait | n/a (framework-managed) |
| Session 21 | 2026-05-22 | pattern | P1 | **#118 Contrat skill (vault) vs contrat backend (code)** — le SKILL.md vault dit CE QUE le LLM produit, le code COMMENT (format JSON). | Hotfix = suffixe code, pas modif vault. | **SKILL.md vault = contrat fonctionnel ; code = contrat d'intégration. Une instruction purement technique reste côté code avec un panneau « ne pas toucher » dans le vault.** | règle-globale + ia + fullstack | ia.md, fullstack.md | fait | n/a (framework-managed) |

## Session 20 — 2026-05-22 — itérations S20→S20.3 + skill-loader

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| Session 20 | 2026-05-22 | problème | P0 | **#108 Timezone Paris : 3 causes → tâches à minuit** — prompt UTC + `isAllDay`/`timeZone` absents de l'API TickTick. | `parisLocalToTickTickFields()` DST-safe + prompt heure locale + flags API. | **API externe date/heure : prompt timezone explicite + body inclut `isAllDay` + `timeZone` IANA. UTC implicite INTERDIT. Tester en condition réelle.** | règle-globale + agents | CLAUDE.md (R8), fullstack.md, ia.md | fait | propag (CLAUDE.md R8) |
| Session 20 | 2026-05-22 | problème | P1 | **#109 Confirmation prématurée sur récap sous-agent** — verbatim « implémente et vérifie avant de confirmer ». | Lecture code + run subset tests + walkthrough avant « fait ». | **Avant tout « fait » : lire 2-3 fichiers + run subset tests + valider 3 scénarios E2E.** | règle-globale + orchestrator | CLAUDE.md (R9), _base-agent-protocol.md | fait | propag (CLAUDE.md R9) |
| Session 20 | 2026-05-22 | insistance | P1 | **#110 Sur-prudence = paralysie (4 questions parasites)** — décisions défaut applicables sans validation. | Appliquer le défaut + signaler. | **Décision à défaut clair ET coût d'erreur faible/réversible → appliquer SANS demander, signaler dans le récap.** | règle-globale + orchestrator | CLAUDE.md (R12), orchestrator.md | fait | propag (CLAUDE.md R12) |
| Session 20 | 2026-05-22 | pattern | P1 | **#111 Preview « Valider/Modifier/Annuler » + patch partiel = standard UX** — Modify = patch partiel LLM, pas retape. | Implémenté Telegram→TickTick. | **Toute création d'artefact LLM-extrait → preview + 3 boutons avant écriture ; Modify = patch partiel via re-prompt.** | pattern + ux | ux.md | fait | propag |
| Session 20 | 2026-05-22 | pref fondateur | P0 | **#112 Thomas refuse 2 systèmes parallèles** — « je ne veux pas maintenir deux systèmes » → tout passe par le hub unique (texte ET vocal). | Vocal branché preview TickTick. | **Si l'archi définit « X = hub unique », TOUS les canaux y convergent. Pas d'exclusion par scope.** | founder-prefs | docs/founder-preferences.md | fait | propag |
